language: ruby
bundler_args: --without development
rvm:
  - 2.2.5
  - 2.3.1
  - jruby-9.0.5.0
  - rbx
matrix:
  allow_failures:
    - rvm: rbx
    - rvm: jruby-9.0.5.0
branches:
  only:
    - master
sudo: false
